language: generic

jobs:
  include:
    - stage: 
      name: "Tasks for push event"
      if: type = push
      before_script:
        - uname -a
      script:
        - echo "This is push event"
        - java -version
    - stage:
      name: "Tasks for cron event"
      if: type = cron
      env: BUILDTYPE="cron"
      script:
        - echo "This is cron event"
        - java -version
        - ls -la
    

script:
  - echo $BUILDTYPE